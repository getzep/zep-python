## Running Chainlit sample app

Requires `OPENAI_API_KEY`, `ZEP_API_KEY`, `ZEP_COLLECTION` environment variables to be set.

```bash
poetry install
```

```bash
chainlit run app.py -w
```

(More detailed docs coming soon...)