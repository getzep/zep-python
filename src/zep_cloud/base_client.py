# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import os
import typing

import httpx
from .core.api_error import ApiError
from .core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .core.logging import LogConfig, Logger
from .environment import ZepEnvironment

if typing.TYPE_CHECKING:
    from .batch.client import AsyncBatchClient, BatchClient
    from .context.client import AsyncContextClient, ContextClient
    from .graph.client import AsyncGraphClient, GraphClient
    from .lookup.client import AsyncLookupClient, LookupClient
    from .project.client import AsyncProjectClient, ProjectClient
    from .task.client import AsyncTaskClient, TaskClient
    from .thread.client import AsyncThreadClient, ThreadClient
    from .user.client import AsyncUserClient, UserClient


class BaseClient:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : ZepEnvironment
        The environment to use for requests from the client. from .environment import ZepEnvironment



        Defaults to ZepEnvironment.DEFAULT



    api_key : typing.Optional[str]
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.Client]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    logging : typing.Optional[typing.Union[LogConfig, Logger]]
        Configure logging for the SDK. Accepts a LogConfig dict with 'level' (debug/info/warn/error), 'logger' (custom logger implementation), and 'silent' (boolean, defaults to True) fields. You can also pass a pre-configured Logger instance.

    Examples
    --------
    from zep_cloud import Zep

    client = Zep(
        api_key="YOUR_API_KEY",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: ZepEnvironment = ZepEnvironment.DEFAULT,
        api_key: typing.Optional[str] = os.getenv("ZEP_API_KEY"),
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.Client] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        if api_key is None:
            raise ApiError(body="The client must be instantiated be either passing in api_key or setting ZEP_API_KEY")
        self._client_wrapper = SyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            api_key=api_key,
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.Client(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.Client(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
            logging=logging,
        )
        self._batch: typing.Optional[BatchClient] = None
        self._context: typing.Optional[ContextClient] = None
        self._graph: typing.Optional[GraphClient] = None
        self._lookup: typing.Optional[LookupClient] = None
        self._project: typing.Optional[ProjectClient] = None
        self._task: typing.Optional[TaskClient] = None
        self._thread: typing.Optional[ThreadClient] = None
        self._user: typing.Optional[UserClient] = None

    @property
    def batch(self):
        if self._batch is None:
            from .batch.client import BatchClient  # noqa: E402

            self._batch = BatchClient(client_wrapper=self._client_wrapper)
        return self._batch

    @property
    def context(self):
        if self._context is None:
            from .context.client import ContextClient  # noqa: E402

            self._context = ContextClient(client_wrapper=self._client_wrapper)
        return self._context

    @property
    def graph(self):
        if self._graph is None:
            from .graph.client import GraphClient  # noqa: E402

            self._graph = GraphClient(client_wrapper=self._client_wrapper)
        return self._graph

    @property
    def lookup(self):
        if self._lookup is None:
            from .lookup.client import LookupClient  # noqa: E402

            self._lookup = LookupClient(client_wrapper=self._client_wrapper)
        return self._lookup

    @property
    def project(self):
        if self._project is None:
            from .project.client import ProjectClient  # noqa: E402

            self._project = ProjectClient(client_wrapper=self._client_wrapper)
        return self._project

    @property
    def task(self):
        if self._task is None:
            from .task.client import TaskClient  # noqa: E402

            self._task = TaskClient(client_wrapper=self._client_wrapper)
        return self._task

    @property
    def thread(self):
        if self._thread is None:
            from .thread.client import ThreadClient  # noqa: E402

            self._thread = ThreadClient(client_wrapper=self._client_wrapper)
        return self._thread

    @property
    def user(self):
        if self._user is None:
            from .user.client import UserClient  # noqa: E402

            self._user = UserClient(client_wrapper=self._client_wrapper)
        return self._user


class AsyncBaseClient:
    """
    Use this class to access the different functions within the SDK. You can instantiate any number of clients with different configuration that will propagate to these functions.

    Parameters
    ----------
    base_url : typing.Optional[str]
        The base url to use for requests from the client.

    environment : ZepEnvironment
        The environment to use for requests from the client. from .environment import ZepEnvironment



        Defaults to ZepEnvironment.DEFAULT



    api_key : typing.Optional[str]
    headers : typing.Optional[typing.Dict[str, str]]
        Additional headers to send with every request.

    timeout : typing.Optional[float]
        The timeout to be used, in seconds, for requests. By default the timeout is 60 seconds, unless a custom httpx client is used, in which case this default is not enforced.

    follow_redirects : typing.Optional[bool]
        Whether the default httpx client follows redirects or not, this is irrelevant if a custom httpx client is passed in.

    httpx_client : typing.Optional[httpx.AsyncClient]
        The httpx client to use for making requests, a preconfigured client is used by default, however this is useful should you want to pass in any custom httpx configuration.

    logging : typing.Optional[typing.Union[LogConfig, Logger]]
        Configure logging for the SDK. Accepts a LogConfig dict with 'level' (debug/info/warn/error), 'logger' (custom logger implementation), and 'silent' (boolean, defaults to True) fields. You can also pass a pre-configured Logger instance.

    Examples
    --------
    from zep_cloud import AsyncZep

    client = AsyncZep(
        api_key="YOUR_API_KEY",
    )
    """

    def __init__(
        self,
        *,
        base_url: typing.Optional[str] = None,
        environment: ZepEnvironment = ZepEnvironment.DEFAULT,
        api_key: typing.Optional[str] = os.getenv("ZEP_API_KEY"),
        headers: typing.Optional[typing.Dict[str, str]] = None,
        timeout: typing.Optional[float] = None,
        follow_redirects: typing.Optional[bool] = True,
        httpx_client: typing.Optional[httpx.AsyncClient] = None,
        logging: typing.Optional[typing.Union[LogConfig, Logger]] = None,
    ):
        _defaulted_timeout = (
            timeout if timeout is not None else 60 if httpx_client is None else httpx_client.timeout.read
        )
        if api_key is None:
            raise ApiError(body="The client must be instantiated be either passing in api_key or setting ZEP_API_KEY")
        self._client_wrapper = AsyncClientWrapper(
            base_url=_get_base_url(base_url=base_url, environment=environment),
            api_key=api_key,
            headers=headers,
            httpx_client=httpx_client
            if httpx_client is not None
            else httpx.AsyncClient(timeout=_defaulted_timeout, follow_redirects=follow_redirects)
            if follow_redirects is not None
            else httpx.AsyncClient(timeout=_defaulted_timeout),
            timeout=_defaulted_timeout,
            logging=logging,
        )
        self._batch: typing.Optional[AsyncBatchClient] = None
        self._context: typing.Optional[AsyncContextClient] = None
        self._graph: typing.Optional[AsyncGraphClient] = None
        self._lookup: typing.Optional[AsyncLookupClient] = None
        self._project: typing.Optional[AsyncProjectClient] = None
        self._task: typing.Optional[AsyncTaskClient] = None
        self._thread: typing.Optional[AsyncThreadClient] = None
        self._user: typing.Optional[AsyncUserClient] = None

    @property
    def batch(self):
        if self._batch is None:
            from .batch.client import AsyncBatchClient  # noqa: E402

            self._batch = AsyncBatchClient(client_wrapper=self._client_wrapper)
        return self._batch

    @property
    def context(self):
        if self._context is None:
            from .context.client import AsyncContextClient  # noqa: E402

            self._context = AsyncContextClient(client_wrapper=self._client_wrapper)
        return self._context

    @property
    def graph(self):
        if self._graph is None:
            from .graph.client import AsyncGraphClient  # noqa: E402

            self._graph = AsyncGraphClient(client_wrapper=self._client_wrapper)
        return self._graph

    @property
    def lookup(self):
        if self._lookup is None:
            from .lookup.client import AsyncLookupClient  # noqa: E402

            self._lookup = AsyncLookupClient(client_wrapper=self._client_wrapper)
        return self._lookup

    @property
    def project(self):
        if self._project is None:
            from .project.client import AsyncProjectClient  # noqa: E402

            self._project = AsyncProjectClient(client_wrapper=self._client_wrapper)
        return self._project

    @property
    def task(self):
        if self._task is None:
            from .task.client import AsyncTaskClient  # noqa: E402

            self._task = AsyncTaskClient(client_wrapper=self._client_wrapper)
        return self._task

    @property
    def thread(self):
        if self._thread is None:
            from .thread.client import AsyncThreadClient  # noqa: E402

            self._thread = AsyncThreadClient(client_wrapper=self._client_wrapper)
        return self._thread

    @property
    def user(self):
        if self._user is None:
            from .user.client import AsyncUserClient  # noqa: E402

            self._user = AsyncUserClient(client_wrapper=self._client_wrapper)
        return self._user


def _get_base_url(*, base_url: typing.Optional[str] = None, environment: ZepEnvironment) -> str:
    if base_url is not None:
        return base_url
    elif environment is not None:
        return environment.value
    else:
        raise Exception("Please pass in either base_url or environment to construct the client")
